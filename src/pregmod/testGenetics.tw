:: test genetics [nobr]

<<set $nextButton = "Back", $nextLink = "Main">>
<<set _seed = 0>>

All slave array slaves MUST be present in genePool or else there is a malfunction.
<br>Name ID Origin
<br><br>

genePool:
<<for _i = 0; _i < $genePool.length; _i++>>
	<br>
	_i $genePool[_i].slaveName $genePool[_i].ID $genePool[_i].origin
<</for>>

<br><br>
<br>Name ID Origin Assignment
<br><br>

slaves:
<<for _i = 0; _i < $slaves.length; _i++>>
	<br>
	<<for _k = 0; _k < $genePool.length; _k++>>
		<<if $slaves[_i].ID == $genePool[_k].ID>>
			<<set _seed = 1>>
			<<break>>
		<</if>>
	<</for>>
	<<if _seed == 1>>
		@@.green;_i $slaves[_i].slaveName $slaves[_i].ID $slaves[_i].origin $slaves[_i].assignment @@
	<<else>>
		@@.red;_i $slaves[_i].slaveName $slaves[_i].ID $slaves[_i].origin $slaves[_i].assignment @@
	<</if>>
	<<set _seed = 0>>
<</for>>